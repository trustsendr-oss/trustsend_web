import { CheckCircle2, MessageCircle, Send, Ticket } from "lucide-react";
import { FormEvent, useState } from "react";
import { Footer } from "../components/Footer";
import { Nav } from "../components/Nav";

const WHATSAPP_NUMBER = "243972716360";

export function Contact() {
  const [ticket, setTicket] = useState<string | null>(null);
  const [subject, setSubject] = useState("");
  const [message, setMessage] = useState("");

  const submit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const id = `TS-${Date.now().toString(36).toUpperCase()}`;
    setTicket(id);
    const text = `Bonjour TrustSend,\n\nTicket : ${id}\nSujet : ${subject}\n\n${message}`;
    window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`, "_blank", "noopener,noreferrer");
  };

  return <div className="min-h-screen overflow-x-hidden bg-white"><Nav /><main className="mx-auto max-w-6xl px-5 pb-20 pt-32 sm:px-6 sm:pt-40"><div className="max-w-2xl"><p className="text-sm font-semibold text-brand">Support TrustSend</p><h1 className="mt-3 text-4xl font-bold tracking-tight text-ink sm:text-5xl">Comment pouvons-nous vous aider ?</h1><p className="mt-5 text-base leading-7 text-muted-2">Créez un ticket pour démarrer une discussion avec notre équipe. Vous serez redirigé vers notre canal WhatsApp sécurisé avec votre référence de ticket.</p></div><div className="mt-12 grid gap-8 lg:grid-cols-[1.4fr_0.8fr]"><section className="rounded-3xl border border-surface-2 bg-white p-6 shadow-sm sm:p-8">{ticket ? <div className="py-10 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-brand" aria-hidden="true" /><h2 className="mt-5 text-2xl font-bold text-ink">Ticket créé</h2><p className="mt-3 text-muted-2">Votre référence est <strong className="text-ink">{ticket}</strong>. Continuez la discussion dans WhatsApp.</p><a className="mt-6 inline-flex items-center gap-2 rounded-xl bg-brand px-5 py-3 text-sm font-semibold text-white hover:bg-brand-dark" href={`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(`Bonjour TrustSend, mon ticket est ${ticket}.`)} `} target="_blank" rel="noreferrer"><MessageCircle size={17} aria-hidden="true" /> Ouvrir la discussion</a><button type="button" onClick={() => { setTicket(null); setSubject(""); setMessage(""); }} className="mt-4 block w-full text-sm font-semibold text-brand hover:underline">Créer un autre ticket</button></div> : <form onSubmit={submit} className="space-y-5"><div><label htmlFor="support-name" className="text-sm font-semibold text-ink">Nom complet</label><input id="support-name" required autoComplete="name" className="mt-2 w-full rounded-xl border border-surface-2 px-4 py-3 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-brand/20" /></div><div><label htmlFor="support-email" className="text-sm font-semibold text-ink">Adresse e-mail</label><input id="support-email" required type="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-surface-2 px-4 py-3 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-brand/20" /></div><div><label htmlFor="support-subject" className="text-sm font-semibold text-ink">Sujet</label><select id="support-subject" required value={subject} onChange={(e) => setSubject(e.target.value)} className="mt-2 w-full rounded-xl border border-surface-2 bg-white px-4 py-3 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-brand/20"><option value="">Choisissez un sujet</option><option>Compte et vérification</option><option>Paiement ou transfert</option><option>Carte virtuelle</option><option>Question commerciale</option><option>Autre demande</option></select></div><div><label htmlFor="support-message" className="text-sm font-semibold text-ink">Votre message</label><textarea id="support-message" required value={message} onChange={(e) => setMessage(e.target.value)} rows={5} className="mt-2 w-full resize-y rounded-xl border border-surface-2 px-4 py-3 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-brand/20" placeholder="Décrivez votre demande sans partager de mot de passe, code PIN ou données de carte." /></div><button type="submit" className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-brand px-5 py-3 text-sm font-semibold text-white transition hover:bg-brand-dark"><Send size={17} aria-hidden="true" /> Créer le ticket et discuter</button></form>}</section><aside className="space-y-4"><div className="rounded-2xl bg-surface p-6"><Ticket className="h-6 w-6 text-brand" aria-hidden="true" /><h2 className="mt-4 text-lg font-bold text-ink">Un suivi simple</h2><p className="mt-2 text-sm leading-6 text-muted-2">Conservez votre référence de ticket pour faciliter le suivi de votre demande.</p></div><div className="rounded-2xl border border-surface-2 p-6"><MessageCircle className="h-6 w-6 text-brand" aria-hidden="true" /><h2 className="mt-4 text-lg font-bold text-ink">Discussion directe</h2><p className="mt-2 text-sm leading-6 text-muted-2">Notre canal WhatsApp permet de poursuivre rapidement la conversation avec l’équipe Support.</p></div></aside></div></main><Footer /></div>;
}
